package chapter_2;

public class Page1 {
    // Every time after we create a file we need to commit
    // Commit, commit, commit
    // I edit Page1( it was with lowercase)
    I want to keep this page
}
